# Railway Nixpacks — keep devDependencies for `tsc` even when NODE_ENV=production
[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
