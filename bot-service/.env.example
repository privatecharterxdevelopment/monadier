# Bot Service — production template (Arbitrum V11 only)

# REQUIRED: Bot wallet — must match `bot` on MonadierTradingVaultV11
BOT_PRIVATE_KEY=0x...

# Optional: fail startup if bot address does not match (recommended)
EXPECTED_BOT_ADDRESS=0xF7072A1067194648f309A215250004abe177531a

# Bot execution — Hyperliquid perps only
HL_AGENT_MASTER_SECRET=0x...
HL_AGENT_NAME=monadier
HL_AGENT_VALIDITY_DAYS=90
HL_MIN_BOT_ACCOUNT_USD=20
HL_BUILDER_ADDRESS=0x64d79e57640A8d4A56Ad1d08c932B5CCF0B263a9
HL_BUILDER_FEE_PERP=30
HL_BUILDER_MAX_APPROVAL=0.1%
HL_OPEN_BUILDER_FEE_PERP=0
HL_SUCCESS_FEE_BPS=1000
HL_MIN_SIGNAL_CONFIDENCE=55
HL_MIN_DIRECTIONAL_TFS=2
HL_MIN_TREND_ALIGNMENT=50
HL_BLOCK_OPPOSITE_SAME_COIN_MS=900000

# Bear-market regime — LONGs only when BTC + ETH clearly UP on 15m + 1h, else SHORT-only
# HL_BEAR_REGIME_ENABLED=true       # default ON
# HL_BEAR_REGIME_CACHE_MS=20000

# Trailing SL — loosened for 40× (avoid scratch exits)
# HL_TRAIL_ARM_MIN_PROFIT_HOLD_MS=720000   # 12 min in profit before arm
# HL_TRAIL_BE_ARM_ROE_PCT=8                # stage 1 breakeven lock
# HL_TRAIL_ARM_ROE_PCT=12                  # stage 2 full trail
# HL_TRAIL_MIN_ACTIVE_MS=480000            # 8 min after arm before close
# HL_PROFIT_TRAIL_MIN_PEAK_FRAC=0.5        # trail gap ≥ 50% of excursion
# HL_PROFIT_PEAK_DROP_FRAC=0.55            # peak-grab after 55% giveback

# Loss protection — only force-close when the user set an SL
# HL_LOSS_CAP_ENFORCE=true          # honor user SL% (default ON); no SL = hold until green
# HL_EMERGENCY_MAX_LOSS_USD=0       # optional USD wipe cap (default OFF)
# HL_MAX_MARGIN_LOSS_PCT=55         # clamp absurd user SL% under liquidation zone
# HL_LIQ_CLOSE_REMAINING_FRAC=0.28  # near-liq close only if user SL is set
# HL_LIQ_CLOSE_WITHIN_PCT=0.004

# REQUIRED: Supabase (service role — server only, never expose to frontend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# REQUIRED: Treasury for fees / subscriptions — must match vault.treasury() on V11
TREASURY_ADDRESS=0x64d79e57640A8d4A56Ad1d08c932B5CCF0B263a9

# REQUIRED: V11 vault on Arbitrum (default = canonical V11)
ARBITRUM_VAULT_ADDRESS=0x7dE97f35887b2623dCad2ebA68197f58F7607854
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc

# Production safety
NODE_ENV=production
ENABLE_DEMO_SIMULATOR=false
LOG_LEVEL=info

# Multi-user scale (1M+ signups — tune per Railway plan)
BOT_USER_CONCURRENCY=64
BOT_MAX_USERS_PER_CYCLE=5000
BOT_GLOBAL_SCAN_CONCURRENCY=24
BOT_SKIP_SUB_BOOTSTRAP=true

# Optional: Bot API auth for Supabase edge verify-bot-trade
# BOT_SERVICE_API_KEY=...

# Win emails (bot trades + betting) via Resend — verified domain required
# RESEND_API_KEY=re_...
# RESEND_FROM=HyperGain <hello@hypergain.io>
# APP_PUBLIC_URL=https://hypergain.io

# AI auto-betting (HIP-4 outcomes)
# HL_AUTO_BETTING_INTERVAL_MS=90000
# HL_AUTO_BETTING_RISK_FRAC=0.05
# HL_AUTO_BETTING_MAX_OPEN=3
# HL_AUTO_BETTING_MIN_LEAN=55
# HL_BETTING_CASHOUT_FEE_BPS=250
# HL_BETTING_WINS_BEFORE_BLOCK=1
