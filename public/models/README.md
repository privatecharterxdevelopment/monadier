# Hero 3D models (Blender → web)

Export from Blender as **GLB** (Draco recommended), then add:

- `vault-hero.glb` — foreground hero sculpture

Wire up in `src/components/landing/HeroVault3D.tsx` with React Three Fiber when ready.
