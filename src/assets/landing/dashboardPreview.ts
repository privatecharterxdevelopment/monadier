/** Full-resolution product screenshot — imported as URL (no re-encode). */
import dashboardPreviewUrl from './dashboard-preview.png';

export const dashboardPreview = dashboardPreviewUrl;
export const DASHBOARD_PREVIEW_WIDTH = 2872;
export const DASHBOARD_PREVIEW_HEIGHT = 1386;
